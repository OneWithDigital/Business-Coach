-- CreateTable
CREATE TABLE "AffiliateOverride" (
    "id" TEXT NOT NULL,
    "url" TEXT NOT NULL,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "AffiliateOverride_pkey" PRIMARY KEY ("id")
);
